c:/dev/ubuntu/4_Install_D_language_EXPERT.md
# 3. Install D Language (Expert)

## Overview
Quick D toolchain setup for experts.

### Steps
- Install DMD and Dub:
  ```sh
  sudo apt install dmd dub
  ```
- (Optional) Use install script or DVM for latest versions.
- Verify with `dmd --version` and `dub --version`.

---
For step-by-step, see the BEGINNER guide.